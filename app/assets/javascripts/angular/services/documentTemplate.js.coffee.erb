<% environment.context_class.instance_eval { include ActionView::Helpers } %>
<% environment.context_class.instance_eval { include HostHelper } %>

App.factory 'DocumentTemplate', ['$resource', 'Auth', ($resource, Auth) ->
  resource = $resource('<%= angularApiPath('/api/documents/templates/:client_id') %>', {format: 'json'})
  return Auth.wrapActions(resource)
]
